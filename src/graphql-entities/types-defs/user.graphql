type User {
  uid: String!
  name: String
  email: String
  avatar_id: Int
  created: Timestamp
  cpf: String
  cep: String
  ponts: Int
  facebook: String
  twitter: String
  instagram: String
  site: String
  youtube: String
  subscriber: Boolean
  is_debater: Boolean
  is_moderator: Boolean
  votes: [ Vote ]
  cards(page: Int!): AllCards!
  reactions: [ Reaction ]
  topics(page: Int!): TopicsByUser!
  deck: [ Deck ]
}

type TopicsByUser {
  count: String!
  records: [ Topic ]!
}

type AllUsers {
  count: String!
  records(page: Int!): [ User ]
}

type AllModerators {
  count: String!
  records(page: Int!): [ User ]
}

type AllDebaters {
  count: String!
  records(page: Int!): [ User ]
}

type TokenObject {
  token: String!
}
